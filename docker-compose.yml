version: '3.8'

services:
  agent-app:
    build: .
    container_name: hackalem_ai_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Позволяет перезагружать код без пересборки контейнера
      - .:/app
    # restart: unless-stopped